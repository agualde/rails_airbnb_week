<div class="container">
  <div class="row">
    <div class="col">
      </div><h1>Welcome to my flats!</h1>

<div class="flat-card">
      <% @flats.each do |flat| %>
      <ul>
        <li><%= flat.title %></li>
        <li><%= flat.description %></li>
        <li><%= flat.address %></li>
      </ul>
      <% end %>

</div>

    </div>
  </div>
